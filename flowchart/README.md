# Ralphex Flowchart

This is a standalone flowchart that explains how Ralphex runs a GPT Codex loop.

## View locally

Open `index.html` directly, or run a tiny static server:

```bash
cd flowchart
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.
